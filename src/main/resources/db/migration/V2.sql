CREATE TABLE IF NOT EXISTS public.contacts(
                                              id INTEGER PRIMARY KEY ,
                                              name varchar(255),
                                              phone varchar(255)
);